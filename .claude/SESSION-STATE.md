<!-- =============================================================================
File: SESSION-STATE.md
Version: 74
Path: .claude/SESSION-STATE.md
Description: Current project state. Single source of truth for "where are we".
             Updated in place at the end of each significant session.
             Read by Claude Code at session start to restore context.

Discipline: this file SHALL NOT exceed 150 lines.
            When approaching the limit, archive the outdated portions into
            a new .claude/sessions/YYYY-MM-DD-<slug>.md entry and trim here.

Autonomous write policy: per CLAUDE.md §9.1, Claude MAY write this
            file autonomously only for trivial deltas (date bump,
            §6 archive append, cosmetic fixes). All other changes
            require explicit user validation of the diff.
============================================================================= -->

# Project State — ay_monorepo

**Last updated:** 2026-08-19 (**LLM provider-independence (D-011) — fully realised.** Removed EVERY Anthropic-specific model entry + `os.environ/ANTHROPIC_API_KEY` from the LiteLLM config (canonical v6 + configmap v4). `model_list` is now 3 provider-NEUTRAL tiers `flagship`/`balanced`/`fast` (`model:"*"` + clientside auth) + the `"*"` catch-all; `agent_routes` reference the tiers. The in-app ENCRYPTED registry resolves each tier → provider endpoint + decrypted key per call (litellm `configurable_clientside_auth_params`, api_base bounded by an anti-SSRF allowlist). **Proven** by a new litellm **testcontainers** test (real proxy + sibling mock upstream). **Option B bootstrap**: seed skips all pass-throughs → registry EMPTY until the operator registers a provider via the HMI (no LLM call works until then). **Cost moved to the registry** (`build_registry_cost_catalog`, keyed `<wire>/<upstream>`, operator-set `provider_cost`; receiver layers it over config per event). `.env.secret.example` (root+overlay v3): `ANTHROPIC_API_KEY` removed, `AY_SECRET_MASTER_KEY` added. Backend CI **2245 passed** (only an unrelated K8s-cert env error, not masked §10). **Deploy pending**: docker-desktop cluster reset left a stale kubeconfig CLIENT cert — needs a full Docker Desktop restart (I fixed the server CA via `kubectl config`, but a client cert can't be forged). Journal `2026-08-19-provider-independence-d011.md`. _(Prior 2026-08-17: real-time agentic transparency ; full record §3/§6.)_)

---

## 1. Current stage

**Étape 1 — Backbone components: DONE.** (C1..C9, C12 shipped + deployable via `e2e_stack.sh dev`.)

**Étape 2a — UX chat polish: DONE.** (Full record in `sessions/2026-05-12-ux-chat-finalisation.md`.)

**Étape 2b — Project artifacts + DocGen : IN PROGRESS (near V1 close).** Pass 1/2.1/2.2 + Generate-E2E + Phase 2.C chat-direct DocGen + Increment 3 (tab-nav state) = DONE. **DocGen versioning + UI tranche = DONE** (2026-05-21) : live-docs per-AI-response version (turn-tagged Gitea commits → `vN`), version-history viewer (`?ref=<sha>` + per-file `git/commits?path`), drag-and-drop tree relocation, expandable chain-of-thought tool detail, versioned "open in working area" links below the response, full-width mouse-resizable 3-pane working area persisted in prefs. Remaining for V1 close — see §5.

---

## 2. Components status

| Component | Status | Notes |
|---|---|---|
| C1 Gateway | **done** | Traefik v3, `routers.yml`. |
| C2 Auth | **done** | prefs (trigram/user_prompt/user_color) + project system_prompt. |
| C3 Conversation | **done** | RAG + chat-direct DocGen tool-loop. SSE unified `event: inline` (now carries per-tool `arguments` + resulting `version`); per-response `X-Turn-Id` for version batching; persisted `MessagePublic.events`. |
| C4 Orchestrator | **done / artifacts in progress** | Run state machine + code plugin + document CRUD (`live-docs` run). Per-file `ArtifactNode.version` + `read_document_at_ref` + `git/commits?path` (R-200-147 history). |
| C5 Requirements | **done (v1.5)** | CRUD + tailoring + history + reindex + export. |
| C6 Validation | **done (v1.5)** | 9 checks (8 real, #3 stub). Graded verdicts T1 + opt-in T3 judge (R-700-032) + quality-regression (R-700-033). |
| C7 Memory | **done** | Federated retrieval, Ollama embedder, hybrid KG. |
| C8 LLM Gateway | **done** | Client + **LiteLLM proxy deployable** (compose + K8s base `c8_gateway`) + client-side per-agent routing (Claude by quality/cost) + cost receiver (`c8_llm/main.py`, COMPONENT_MODULE=c8_llm) → Arango `llm_calls`. Single key `C8_GATEWAY_API_KEY`. |
| C9 MCP | **done** | 8 tools. |
| C12 Workflow | **deployed** | n8n via Traefik. |
| **UX (Next.js)** | **done (chat+DocGen)** | Profiles code/docgen. Working area 3-pane + Documents tree. `<InlineLog>` unified renderer. |

---

## 3. Active decisions (beyond specs)

- **Architecture** : Python 3.13 src layout. Monorepo. B1 (single `ay-api:local` × N via `COMPONENT_MODULE`). Single Arango `platform`. LiteLLM=C8 HTTP-only (R-800-011).
- **Governance** : CLAUDE.md v16 + `.claude/settings.json` v14. §10 test-debug / §11 coverage / §5.7 shell / §4.6 env-tiers / §5.2 sed-ban.
- **Validation philosophy** (2026-05-19, §5.3 v16) : human-in-the-loop applies at **decision gates** (architecture, plans, todos, semantic env changes per §4.6, new specs, contract changes), NOT at **execution gates** for read-only / test / lint / build / analysis commands. The `settings.json` allow-list scope reflects this distinction and expands over time as new lecture-only / test-only commands prove safe and frequent. Composed shell (`&&`, `2>&1 | tail`, heredoc-with-write) remains §5.7-banned regardless of whether the underlying tools are allowlisted — the matcher sees the chain as one unit.
- **Catalog-driven CI** : `tests/e2e/auth_matrix/_catalog.py` SOT ; coherence pins route↔catalog↔coverage.
- **UX architecture** : runtime-config 2 tiers ; `(protected)/` auth gate ; build-stamp footer ; session-revoked redirect.
- **Artifacts UX** (2026-05-12) : transparent backend (MinIO/Gitea proxied) ; profile-aware section ; MinIO `orchestrator/c4-artifacts/{tenant}/{project}/{run}/{path}`.
- **D-015 DocGen v1 = chat-direct** (2026-05-16) : `create/update/read/list/delete_document` tools mutate the artifact surface from C3 chat. v2 = synthesis-v4/OpenHands (future). ADR in `999-SYNTHESIS.md`.
- **Live-docs versioning = batched per AI response** (2026-05-21, R-200-147) : C3 mints one `response_turn_id` per turn → forwarded as `X-Turn-Id` → C4 embeds `[turn:<id>]` in the Gitea commit message. `ArtifactNode.version` = count of DISTINCT turn ids in a file's history (N writes in one response = one bump). History viewer reads content at a SHA via Gitea `contents?ref=` (`read_document_at_ref`) ; MinIO keeps only latest. Stateless (derived from Gitea, no new store). Inline log is pure chain-of-thought ; modified-doc deep-links moved below the response with `(vN)`.
- **Unified inline-event pipeline** (2026-05-19) : `StageRecord`+`ToolCallRecord` → one `InlineEvent` (discriminated `kind`) ; one persisted `MessagePublic.events` audit ledger (read-time shim projects legacy v3 `stages`, no data migration) ; one SSE channel `event: inline` ; one `<InlineLog>` formatter-registry (add a kind = add a formatter). Registered-contract change (§8.4) ; tests adapted (§10.4).
- **C8 LiteLLM proxy + per-agent routing + cost tracking DONE** (2026-05-22, V2 #1, R-800-001/070) : off-the-shelf LiteLLM, client-side `agent_routes` → Claude tiers. Single `C8_GATEWAY_API_KEY`. Cost forwarder → Arango `llm_calls`.
- **V2 #2 OpenHands `generate` POC adapter** (2026-05-22) : gated `pipeline/generate_engine.py::OpenHandsGenerateEngine` (opt-in `C4_GENERATE_ENGINE=openhands`, default `in_process` UNCHANGED), OpenHands V1 SDK via C8/LiteLLM. Gate B blocks by design (Q1 unresolved). R-200-029..038 PROPOSED, no `@relation` claimed.
- **V2 #3 = Graphiti-STYLE memory, ArangoDB-native** (2026-05-22) : Implements D-016 pattern natively in Arango (NOT the lib — would break D-002). Sequence A→B→C. B (L2/L3, R-400-205/206) D-010-gated ; C (bi-temporal) was §8.1 spec gap → D-019. KG records carry `layer`/`ontology_version` so B/C additive.
- **V2 #3 Block A — D-016 v1 subset DONE** (2026-05-22→23, R-400-200..203, E-400-006) : closed ontology + L1 structural extractor + hybrid retrieval (BM25+dense+RRF) + chunk contextualisation. Journals 2026-05-22/23.
- **Eval harness + C bi-temporal + D-017 C6 slice DONE** (2026-05-23, R-400-209 / R-700-032 / R-700-033) : tests/eval/ recall@3=1.0 on golden → D-010 closed (B not justified). Bi-temporal KG (valid + transaction time, append-only, as-of queries). T3 LLM-judge opt-in + quality-regression anti-feedback. #3 `interface-signature-drift` deferred.
- **Dev stack = real LLM ; CI = mock/Ollama** (2026-05-26) : `e2e_stack.sh dev` (manual user tests) now brings up the LiteLLM proxy (`--profile litellm`, v7) → C8→Claude production reality ; `up`/`full`/pytest (CI) stay mock_llm + Ollama (zero provider cost). Two runtime defects fixed (uncaught by CI — container-only) : (1) C7 loaded the tree-sitter parser AT IMPORT → the `--no-create-home` container user had no writable grammar cache → boot crash cascading to C3 ; now LAZY (`functools.cache`, `code_extractor.py` v2). (2) litellm crashed on first real boot — it resolves a `callbacks` entry as a FILE next to the config, not via PYTHONPATH → now bind-mount `/app/cost_forwarder.py` (override v11). Runtime sequence diagram `051-RUNTIME-execution-flow.svg` added (RAG chat turn + DocGen tool-loop, colour = acting component) ; `050-WORKFLOW` blueprint now partially stale (A.b/A.c/eval done). End-to-end chat still depends on `.env.secret` keys being valid.
- **Platform LIVE on local Kubernetes DONE** (2026-05-27, docker-desktop) : first k8s bring-up, 8 latent manifest bugs fixed (namespace conflict, c4-orchestrator SA location, minio root user, litellm OOM+callback mount, Traefik --ping+nodes RBAC, IngressRoute drift). LoadBalancer-on-Traefik dev overlay → `localhost:56000`. New tooling `e2e_stack.sh build` (v8) + `run.sh --crds` (v2). 15 pods Ready, 19 routers. Journal `2026-05-27-k8s-local-deployment.md`.
- **Documents file-manager feature DONE** (2026-05-28) : R-500-010 v2 — shared `LiveDocsManager` (~330 LOC) wired into BOTH Documents tab + Working area (3 gaps : toolbar `+ New file/folder`, empty-state, inline Edit/Save). apiClient `getDocumentText`/`createDocument`/`updateDocument` + 5 unit + 3 integration tests. 131/131 vitest green ; lint + typecheck verts.
- **D-020 AyExtractor adopted as dependency component C13** (2026-05-28, spec-only session 1/7) : prior internal work `ay_extractor/` becomes the platform's source extraction + chunking engine, deployed as new dependency component **C13** (vendored monorepo, local wheel build, HTTP-only surface `POST /analyze` + `GET /status/{run_id}` + `GET /healthz`, zero `import` coupling with `ay_platform_core/`, LLM routing via C8 by env switch `OPENAI_BASE_URL`). **Re-partitions D-013** : R-100-081 v1→v2 — C12 (n8n) owns trigger + orchestration, **C13 owns extract+chunk+write MinIO artifacts** (markdown + jsonl + `run_manifest.json` stamped with `ayextractor_version` + per-agent `llm_assignments` + `prompt_hashes` for byte-exact reproducibility + run diff), C7 owns embed + index only. **v1 scope** = Phase 1+2 only ; Phase 3 (concepts/triplets/Leiden/profiles/synthesis) deferred to Q-200-022. **LLM-frugal** : `quality_tier ∈ {minimal | standard | high}` per project (default `minimal` = zero LLM cost when no images), image_analyzer mandatory (only when images), decontextualizer/Refine/Chain of Density opt-in. **Strip mandatory** : 10+ AyExtractor modules physically removed (`rag/{retriever,vector_store,graph_store,enricher,indexer,embeddings}`, `consolidator/`, `batch/`, `graph/`, Phase 3 agents, alternative cache backends, alternative LLM adapters, local/s3 writers). New endpoint C7 `POST /memory/projects/{pid}/sources/{sid}/ingest-chunks` accepts ChunkRich (text + decontextualised? + context_summary? + global_summary? + section_path + char offsets + references + tokens + extraction_run_id). Implementation: 6 sessions (strip → adapters → HTTP wrapper → C7 endpoint → n8n workflow → cleanup). 339 requirements indexed by `060-IMPLEMENTATION-STATUS.md`.
- **Increment 3a/3b — cross-nav UI store + provider SSE loop** (2026-05-19, DONE) : `WorkspaceProvider` per-project Tier-1 store (per-conv composer drafts, sessionStorage v2 normalised). Provider-owned SSE runtime (`useConvRuntime` via `useSyncExternalStore`) so a stream re-renders ONLY the active chat ; live generation survives unmount. Wired on ChatSidebar + Conversations `[cid]`. Full detail in `sessions/2026-05-19-increment-3-...`.
- **Source upload : 405-fix → contract-test tier → D-020 RE-PARTITION (byte custody C12→C7, R-100-081 v3) DONE & live-validated** (2026-05-29 → 06-01) : the 405 was a front/back contract drift (UI hit a D-020-removed C7 route) ; guarded by a NEW anti-drift contract-test tier (`live routers → _catalog.py → backend-routes.json snapshot → api-surface.test.ts` drives EVERY apiClient method ; `test_ui_contract_snapshot.py` pins it). The deeper fix re-partitions D-020 ingestion (operator option C) : **C7 owns the raw-blob MinIO write + C12 trigger + artifact read** (`POST /sources/upload` multipart → store + trigger C12 async + 202 ; `/ingest-chunks` v3 reads chunks.jsonl/manifest from MinIO) ; **C12 = pure metadata orchestrator** (workflow v3 = 7 nodes, `onReceived`, no MinIO/decode) ; n8n's S3/binary limits no longer apply (no base64/anonymous-bucket). UI `uploadSource` v9 → multipart. 108 c7 unit+contract green ; coherence 25/25 (catalog +endpoint, snapshot 102) ; UI 73 + gate 4/4 ; **live Playwright multipart upload → C7 → C12 → 202 PASSED**. Real extraction needs litellm/dev. Journals `2026-05-29-upload-405-...` + `2026-06-01-d020-repartition-byte-custody-c7.md`.
- **C13 enrichment ACTIVATED + per-project config + Claude routing (R-400-224) DONE** (2026-06-02) : enrichment was scaffolded-but-dormant ; now wired. **P1** dedicated per-chunk orchestrator `pipeline/enrichment.py` (the agents take per-chunk `*Input`, NOT `PipelineState` → the generic `DocumentPipeline` runner was incompatible) ; `resolve_enrichment(tier, ConfigOverrides)` = preset OVERRIDDEN by per-option flags ; references + honest manifest. **P2** `extraction/image_pipeline.py` : real image bytes (PDF `extract_image`/DOCX `target_part.blob`) + **sha256 dedup** + vision caption (`analyze_image`) via `llm_factory("image_analyzer")` — model **independent** of the text agents (`llm_assignments`) ; per-image `01_extraction/images/img_{sha8}.json`. **P3 decision : C7 owns the config** (not C2) — `EnrichmentConfig` + collection `memory_project_config` + GET(authenticated)/PUT(owner) ; `store_raw_upload_and_trigger` forwards `quality_tier`+`config_overrides` ; n8n workflow forwards them ; Settings UI section (tier + tri-state toggles + image-model field). **P4** UI decontextualization before/after + summary/image `EnrichmentDigest`. **Dev routing: C13 enrichment → Claude via C8/LiteLLM** ; embeddings stay Ollama (NEW `embedding_base_url` split) ; `e2e_stack.sh` v10 (.env.secret env-file + c12 restart, §5.3) ; devcontainer editable-installs C13 (Dockerfile v1.11, deps scikit-learn/ebooklib/bs4 declared). Cleaned ~20 stale D-020 C13 tests (§10). 417 C13 unit + backend All-OK + UI 372/4-thresholds ; catalog 111. Journal `2026-06-02-c13-enrichment-activation.md`.
- **RAG quality + ingestion cost optimisation DONE** (2026-06-03) : **Contextual retrieval** — embedding + ArangoSearch BM25 augment with `section_path` (`search_text`, both arms context-aware) ; `global_summary` de-duped to the source row (`document_summary`) ; `ChunkContent` + UI show the full retrieval payload ; c3 stops truncating RAG chunks to 800 chars (severed self-containment). **Structure-aware chunking option B** : `structure_detector` real section extents (fixed `text.find`) + `StructuralChunker` v2 cuts at section boundaries, coalesces sibling subsections under a shared parent, `section_path`=longest-common-ancestor. **Legacy audit** : 7 stale D-020 C13 integration failures fixed (removed-field refs §10.4-D) + **dead code removed** (`pipeline/document_pipeline.py`+`runner.py` — referenced only in comments + their own tests, §11.2). **Cost ($4.53→~$1)** : #1 C13 enrichment+vision → **Haiku** (agent_routes + dev env, ~73 %) ; #2 caption `max_tokens` 512 + skip <3KB decorative images (client resize dropped — Anthropic caps ≤1568px server-side) ; #3 **map-reduce summariser redesign** (stateless `text→summary` agent ; batched parallel MAP + hierarchical REDUCE ; decon parallelised, uses the document summary) ; #4 **reusable prompt-cache primitive** (`cache_system` on C13 `OpenAIAdapter` + `cache_hint=static`→`cache_control` in platform C8 client — opt-in, no-op below Haiku-4096/Sonnet-1024, NOT wired on tiny-prompt agents). Per-phase progress logs (`enrichment.map/reduce/...`) + `e2e_stack.sh restart-llm` (§5.3). 548 C13 + backend All-OK + UI 373/gate. Journal `2026-06-03-rag-quality-and-cost-optimization.md`.
- **RAG follow-ups (A1/B/C) + LLM-governance design & crypto brick DONE** (2026-06-04) : **A1** image captions folded INTO the text before chunking (facade reorder → chunks + summaries incorporate image content ; A2 positional anchoring deferred). **B** per-phase observability `00_metadata/run_stats.json` (duration + LLM calls + tokens per phase + totals) + UI table. **B-ii** authoritative **$ cost via C8** : C13 sends `X-Run-Id`/`X-Source-Id`/`X-Tenant-Id`/`X-Project-Id` headers → the mounted cost-forwarder already captures them → `CallTags.source_id/run_id` (`_extract_tags`) → C7 `source_enrichment_cost` SUMs `llm_calls.cost_usd` by `tags.source_id` → `SourceDiagnostics.enrichment_cost_usd` + UI (snapshot-priced at record time). **C** document summary surfaced on `standard` (`global_summary` = dense ELSE map-reduce refine). **LLM-governance feature DESIGNED** (multi-tenant) : 3 layers — platform registry (models + **encrypted** API keys + provider cost + capabilities + default `model_quality`) → tenant catalog (curated subset + chargeback rate-card, NO keys) → project picks `model_quality` low/med/high. **Locked decisions** : app-level registry / NO BYO-key ; `model_quality` global per project (≠ `EnrichmentConfig.quality_tier` depth) ; **NO KMS** — Master Key via ENV var (Vault-backable), ciphertext in DB, single app key v1 ; **option B** (HMI-editable keys, C8 injects decrypted key per call — pending a litellm spike) ; LLM API keys only ; passwords stay Argon2id (C2). **First brick SHIPPED** : `crypto/SecretCipher` (AES-256-GCM, env keyring, AAD `llm_registry:<alias>:api_key`, rotation, token `ay.1.<kid>.<nonce>.<ct>`, `masked_suffix`) — 13 tests, ruff+mypy clean. Backend All-OK (1748) + C13 551 + UI 373/4-4. **Handoff brief written.** Journal `2026-06-04-rag-followups-and-llm-governance-plan.md`.
- **LLM-governance operator console — Lots 1·2·3 + self quota view DONE** (2026-06-08) : **L1** platform LLM **registry CRUD** (DELETE endpoint + add-model HMI + sortable table default quality-asc) + **api_base** stored & injected per-call (`UpstreamConnection` key+api_base ; litellm v5 `configurable_clientside_auth_params:["api_key",{api_base:<allowlist-regex>}]` anti-SSRF ; **api_base injectable without a master key** → `build_registry_key_provider` always returns a provider) + super-root `/projects` 403→admin-home. **L2** **E-100-002 v2→v3** : `tenant_manager` = **platform operator** (NOT content-blind-only) — tenant **(de)activation** (`issue_token` refuses a deactivated tenant's members ; missing tenant doc = active so super-root login never regresses) + **cross-tenant user oversight** (`GET /admin/users?tenant_id=`, `POST /admin/users/{id}/(de|re)activate`) ; user create/delete stays the tenant admin (content). `TenantPublic.active` + repo `update_tenant`/`list_users`. **L3** **global LLM quota policy** (`c8_llm/quota/`) : ONE platform `QuotaPolicy`, rolling windows (default session-5h/week/month, durations+limits parametrable), limits in **cost($) AND tokens** (first reached triggers), usage summed from `llm_calls` per tenant per window (no new store), **soft→hard** (`evaluate`→ok/warn/exceeded ; `QuotaGuard` wired in C3/C4/C7 beside the key provider → raises `QuotaExceededError` at the gateway BEFORE spend) ; defaults inert (open limits). c8_admin `GET/PUT /admin/v1/quota/policy` + `GET /admin/v1/quota/status`. **Self view** : `GET /api/v1/quota/me` (ANY authed user, own tenant via `X-Tenant-Id`) + navbar **`QuotaIndicator`** pill (poll 60s, most-constrained window, click→per-window popover) visible everywhere. **HMI under `/operator/{tenants,users,quotas}`** (UI-owned prefix → catch-all, NO per-page Traefik route ; `/admin/tenants|users` ARE backend paths → hard collision avoided) ; registry HMI stays `/admin/llm-*`. New Traefik routes `c8-admin-quota` (`/admin/v1/quota` prio100) + `-quota-self` (`/api/v1/quota` prio50). Spec `100-SPEC v16`/E-100-002 v3 + `800-SPEC v5` (quota) ; `065` 119→128 ; coherence + auth-matrix extended. `settings.json` v20 (seed + 065-gen wrappers allowlisted). **Backend All-OK 1931 + UI 47/410 > 4 thresholds.** Journal `2026-06-08-llm-governance-lots-1-2-3.md`.
- **LLM provider-independence (D-011) fully realised** (2026-08-19) : the platform had a residual **Anthropic adherence** (static `anthropic/…` model_list entries + `os.environ/ANTHROPIC_API_KEY` in the LiteLLM config, Anthropic-named `agent_routes`, cost from the config catalog) despite D-011. Closed in 5 verified blocks. **B1** — proxy honours per-call creds: `model_name: "*"` catch-all + `configurable_clientside_auth_params` (`api_key` + anti-SSRF `api_base` allowlist), **proven** by a NEW litellm **testcontainers** test (real proxy + sibling mock upstream, DooD-safe base64-in-container config; asserts the injected per-call `api_key`+`api_base` reach the upstream and a non-allowlisted base is refused). **B2+B3** (coupled — `validator.py` requires each `agent_routes` target to exist in `model_list`): replaced the 3 Anthropic entries with 3 provider-NEUTRAL tiers `flagship`/`balanced`/`fast` (`model:"*"` + clientside auth + full feature set), routes reference the tiers, removed all `os.environ/ANTHROPIC_API_KEY`. **B4 (Option B)** — seed skips every pass-through (`model=="*"`) → registry EMPTY at boot → operator registers a provider + maps tiers via the HMI (no LLM call works until then). **B5** — cost moved to the registry: `build_registry_cost_catalog(models,providers)` keys `<wire>/<upstream>` → operator-set `provider_cost`; the cost receiver layers it OVER the config catalog per event (HMI change effective immediately). Canonical config v6 / configmap config v4 / `seed.py` v3 / `cost_tracker.py` v4 / `c8_llm/main.py` v2. `.env.secret.example` (root+overlay v3): `ANTHROPIC_API_KEY` removed, `AY_SECRET_MASTER_KEY` added (encrypts registry-stored provider keys). Tests updated to the new contract (§10.3-D, flagged): seed→empty + synthetic-config mechanism, config-schema/cost-receiver neutral tiers, app-factory empty registry, cost-receiver + quota-attribution cost from a seeded registry model. **CI 2245 passed** (only `test_k8s_dispatcher_e2e` fails — unrelated K8s-cert env error, not masked). Deploy pending a docker-desktop kubeconfig client-cert refresh (full app restart). Journal `2026-08-19-provider-independence-d011.md`.
- **Real-time agentic transparency + per-request cost breakdown DONE** (2026-08-17) : a Claude-Code-like exhaustive "watch the agent work" view, delivered in 3 verified/deployed increments over the append-only TraceEvent/InlineEvent stream (R-200-206/207/208, 200-SPEC v4 ; R-800-146/147, 800-SPEC v12). **Inc 1** live SSE runs : C4 `GET /api/v1/orchestrator/runs/{id}/events` replay-then-tail over the TraceEvent ledger (`get_full_trace → (events, RunStatus)`, done when run ≠ RUNNING) ; UI `streamOrchestratorEvents` (fetch `body.getReader()`, not EventSource — needs forward-auth headers) → `liveTrace` preferred over polled `run.trace` on the pipeline page. **Inc 2** extended thinking + verbosity toggle : C8 `_apply_adaptive_thinking` (`thinking:{type:"adaptive"}`, no-clobber) via a `reasoning_verbose` flag on `chat_completion(_stream)` ; C3 `_extract_delta_reasoning` (`delta.reasoning_content`) → per-chunk `reasoning` inline events + a persisted final one ; MessageRequest `reasoning_verbose` + C2 pref `reasoning_verbosity ∈{normal,verbose}` ; UI 🧠 composer toggle + violet `ReasoningFormatter`. **Inc 3** sub-agent tree : `TraceEvent.sub_agent_id`/`parent_agent` tagged on both agent-dispatch markers ; UI pure `buildSubAgentTree` (group by sub_agent_id, nest by parent, run-level aside) + `<SubAgentTree>` + Timeline/Tree toggle. **Part B — per-request cost by model** (stored+viewable) : `CallTags.turn_id` ; C8 client `_headers` emit X-Run-Id/X-Turn-Id (was C13-only) + sub-agent runtime forwards `run_id` so C4 runs tag spend ; quota `RequestCostBreakdown`+`ModelCostShare`, repo `breakdown_by_model` (AQL COLLECT by model, field whitelist run_id/turn_id) ; `GET /admin/v1/quota/requests/{correlation}/breakdown?by=run|turn` + `/operator/quotas` lookup panel. Inline `reasoning` rides the existing `event: inline` pipeline (one kind + one formatter), no new channel. R-200-206/207/208 + R-800-146/147 = **tested** (060 regen). Backend All-OK + UI 435/4-thresholds ; K8s rebuilt+redeployed each block. Journal `2026-08-17-realtime-agentic-transparency.md`.
- **Operator resource dashboards (cost + storage + rights) DONE** (2026-08-04, E-100-002 v7) : the operator console gains **LLM cost** + **disk storage** + **access** visualisation, scoped (admin=own tenant via `X-Tenant-Id`, platform_manager=all). **Cost** (c8 quota) — `consumption_by_project`/`consumption_by_user` group `llm_calls` by `tags.project_id`/`user_id`; `project_/user_/tenant_consumption_report_days` sum cost+tokens across **day/week/month/quarter/semester/year** (new `day` window beside R-800-144's `session..year`); `GET /admin/v1/quota/consumption/{projects,users,tenants}` (operator-scoped; tenants report platform_manager-only). **Storage** (NEW `c8_llm/storage/`, hosted by c8_admin) — `StorageMeter` discovers `(tenant,project)` from the `c4-artifacts/{tenant}/{project}/` MinIO layout + sums object sizes; `storage_snapshots` time-series (bi-temporal points); live `project_/tenant_storage_report` + `project_series` + `run_snapshot`; `GET /admin/v1/storage/{projects,tenants}` + `.../series` (operator) + `POST /admin/v1/storage/snapshot` (platform_manager); **`c8-storage-metering` CronJob** (every 6h, direct MinIO+Arango, no HTTP/auth) + a UI "Snapshot now" trigger; c8-admin gets MinIO env (endpoint+bucket; creds via aywizz-secrets); absent MinIO → 503, nothing else changes. **Rights** — reverse ACL view `GET /admin/users/{id}/projects` (reuses `get_project_scopes`); UI "View projects" per user. UI cost/storage columns on Projects+Tenants+Users + a day→year cost table + storage series in the expanded project row. **Storage per USER is N/A by design** (files belong to projects). **Also fixed 2 GitHub CI failures** (warnings-as-errors): `testcontainers.arangodb` deprecation → `testcontainers.community.*` w/ fallback ; port-forward `stderr` fd leak → close in teardown. `settings.json` **v22** `defaultMode=acceptEdits` (deny-list unchanged). 065=**152**. Backend All-OK + UI 4-thresholds ; K8s rebuilt+redeployed (c8-admin + CronJob). **Traceability**: R-800-145 (cost dashboards) + R-100-140 (storage metering) with `@relation` markers ; 800-SPEC v10→v11, 100-SPEC v20→v21. Journal `2026-08-04-operator-cost-storage-rights-dashboards.md`.
- **RBAC v7 (content-blind admin) + LLM catalogue opt-out DONE** (2026-07-28) : **E-100-002 v6→v7** — dropped `tenant_manager` ; **`admin` (= `tenant_admin`) = content-blind, tenant-scoped mirror of `platform_manager`** : manages users + projects + ACL + consumption **within its own tenant only**, and reaches **NO project content** (content via `project_owner/editor/viewer` only). Enforcement : content routers C4-C7 strip `admin`/`tenant_admin` before content gates (`_CONTENT_BLIND_GLOBAL_ROLES`) ; `admin`-only content ops (run resume, KG embed, memory refresh) → `project_owner` ; `admin_router` v3 opens user oversight + governance to admin via `_require_operator`/`_require_user_operator` (own-tenant, 403 cross-tenant) ; UI gives admin the operator Projects+Users console (backend-scoped), classic `/projects` hidden for operators, platform-only surfaces stay `platform_manager`. **Security fix (found mid-refactor)** : C2 `/auth/users` CRUD (create/get/update/delete/reset-password) was gated by ROLE only with **no tenant scoping** → an admin could provision/modify/delete/reset **another tenant's** users (account takeover + priv-esc) ; now create forces the caller `tenant_id` and `{id}` routes 403 on a cross-tenant target (`router.py` v6, `_require_same_tenant_user`). **THEN LLM tenant catalogue = OPT-OUT** (**800-SPEC v9→v10**) : lazily materialised with every registry model on a tenant's first touch + per-tenant `initialized` marker (`tenant_llm_catalog_meta`) so "remove all" isn't re-populated ; new `GET /api/v1/llm/catalog/available` re-add picker (public projection, no key) ; UI id-input→picker. Fixes the empty-catalogue dead-end (registry list is platform_manager-only) that also left `model_quality` resolution with nothing to resolve ; isolation now = customisation doesn't leak (shared registry baseline). 100-SPEC v20 (6→5 roles) ; 065=144 ; auth-matrix + snapshot + api-surface updated. Backend All-OK (2141) + UI 424/4-thresholds. K8s rebuilt+redeployed (+C2 re-seed). Journal `2026-07-28-rbac-v7-content-blind-admin-and-catalogue-opt-out.md`.
- **Project governance + role rename `platform_manager` DONE** (2026-07-28) : cross-tenant **project governance** for the platform operator (E-100-002 **v4**) — `/admin/projects` list/status(active/inactive/archived)/ACL + `c2_audit` ; UI `operator/projects` (status+ACL+consumption via quota `?project_id=`) ; **lifecycle enforcement** URL-scoped at `/verify` (method-aware) + record-derived for C3 conversations (read-only `c2_projects` status probe, R-100-012). Then the top global role **`tenant_manager` → `platform_manager`** (E-100-002 **v5**, it is platform-wide not tenant-scoped) : 74-file scripted rename (§5.2 exception, `git diff` review), enum+value+gates+catalog+UI+env+specs+test-filename ; generated regenerated ; dev re-seed for the running cluster. Tooling `run.sh --restart` + `k8s_reseed_c2.sh` ; `settings.json` v21. 100-SPEC v18. NOT renamed (accurate history) : `.claude/sessions/*`, this file's §3/§6 prior entries, CLAUDE.md §13. Backend All-OK + UI 421/4-thresholds. Journal `2026-07-28-project-governance-and-platform-manager-rename.md`.

---

## 4. Open questions

- **600-SPEC** scaffold (code-domain quality engine).
- **C5** : import 501, ReqIF/point-in-time v2. **C7** ML adapters optional extras. **C6** : #3 `interface-signature-drift` stub deferred (load-bearing probe) ; T3 judge D-011 cross-family = open gap (needs a 2nd provider family).
- **Q-100-016/017/018** : trace into K8s Jobs ; workflow-synthesis sampling/retention ; dashboard layer.
- **Q-100-019** : Turbopack incompat → `next dev --webpack`.
- **Q-100-020** : Gitea service-account credential storage → KMS/vault at prod.
- **Q-100-022** : per-agent C8 routing is **STATIC** — each pipeline role maps to a fixed Claude tier (`agent_routes`, by quality/cost a priori : architect→Opus, planner/implementer/reviewers→Sonnet, sub-agent→Haiku). **Dynamic, per-task** model selection (the orchestrator picks the tier from the task's analysed difficulty/need at runtime) is NOT implemented. The hook exists (`payload.model` wins over the route in `_resolve_model` ; `AgentOverride` is open-schema). Improvement, not a defect — design it when role-routing proves insufficient.
- **Q-100-023** : `overlays/prod` still references `../../base` wholesale → the same `c4-workers` Namespace conflict latent there. Proper fix before prod : move `c4_workers` out of base into a standalone opt-in layer applied separately when `C4_DISPATCHER=k8s`. Cost : small refactor (~3 files).
- **Q-200-022** : Phase 3 AyExtractor agents (concept_extractor / community_summarizer / profile_generator / synthesizer / critic) adoption timing + placement (kept inside C13 or merged into C7's existing `kg/` per D-016). Out of v1 scope ; gated by production demand + ROI evidence.
- **Q-200-023** : AyExtractor cache fingerprint backend — port the JSON cache store to MinIO (current AyExtractor design) vs. promote it to an ArangoDB collection alongside `memory_chunks` ? File-based is simpler ; ArangoDB integrates better with the platform's lifecycle. v1 defers cache (rely on R-100-085 sha256 file-level dedup).
- **Q-200-024** : AyExtractor versioning model — keep vendored monorepo + local wheel build (D-020 default), OR split to a separate Git repo with pip-install-from-URL, OR publish to PyPI. v1 = vendored ; v2 reconsider if AyExtractor evolves on a different cadence than the platform.
- **Q-200-025** : C13 integration test strategy — testcontainers (real MinIO + C8 mock for LLM calls) + e2e on the full C12→C13→C7 chain, OR unit-only with fake adapters ? Decided session 3 once the strip + adapter work settles.

---

## 5. Next planned action

Doing the three V2 features in order (operator-confirmed) : **#1 LiteLLM ✓ → #2 OpenHands ✓ → #3 Graphiti-style memory (in progress)**.

**V2 #2 (OpenHands `generate` POC adapter) = DONE** (2026-05-22, see §3). Operator runs the actual Q13 POC (cluster + provider budget, out of sandbox) ; the §8.1 spec amendment (R-200-029..038 into 200-SPEC) follows POC success.

**V2 #3 (Graphiti-style memory, ArangoDB-native)** — Block A (D-016 v1 subset) ✓ + eval harness (D-017 slice) ✓ + C bi-temporal (D-019) ✓ (2026-05-23, CI 1630 green ; see §3). Clean pause point.

Remaining V2 #3 (all gated / queued — not "just go") : **B** (L2/L3 traversal, R-400-205/206) **D-010-gated & NOT justified** by the golden-set eval ; **golden-set expansion** to probe D-010 at production scale ; **full D-017** (#3 de-stub + T3 D-011 cross-family 2nd provider + 600/800 eval slices) ; **C follow-ons** (bi-temporal for the open-domain extractor + chunk valid-time).

**Multi-tenant LLM-governance = DONE** (registry + encrypted keys + tenant catalog + `model_quality` + HMI ; then the **operator console Lots 1·2·3 + self quota view**, 2026-06-08 §3). Operator sets real values to exercise it : `AY_SECRET_MASTER_KEY` in `.env.secret` (enables api_key injection ; api_base already works key-less), and a small quota limit in `/operator/quotas` to see soft→hard + the navbar pill change colour.

**Immediate next (operator's pick)** — candidates : the **429 UX polish** (map `QuotaExceededError` to a clean user-facing message in the conversation, not a raw error) ; **per-call DYNAMIC model selection** (Q-100-022) ; the deferred ops items : (a) **D-020.5 batch API** `urgency=background` (-50% Phase 2 LLM) ; (b) **resume-from-phase** R-400-225 ; (c) **C13 k8s overlay in `overlays/dev`** + in-cluster smoke ; (d) **Q-100-023** refactor `c4_workers` out of `base/` for `overlays/prod` ; (e) **C13 Phase 3 KG** (Q-200-022) when ROI justifies.

**V1 remainder still open (parallel track)** : C6 stub #3 ; prod HTTPS + **K8s prod overlay (Q-100-023 : refactor c4_workers out of base)** + K8sDispatcher wired + CI GitHub Actions. (K8s LOCAL DEV done 2026-05-27 ; LiteLLM deploy done.)

**Reserve (intellectual honesty)** : the Anthropic API is **paid** — every routed turn bills tokens (Haiku keeps it cheap). `.env.secret` is git-ignored. Ollama remains the offline/free fallback (revert `C8_GATEWAY_URL`).

---

## 6. Sessions archive

Latest entries (most recent first):
- `.claude/sessions/2026-08-19-provider-independence-d011.md` — **LLM provider-independence (D-011) fully realised**. Removed all Anthropic model entries + `os.environ/ANTHROPIC_API_KEY` from the LiteLLM config; `model_list` = neutral tiers `flagship`/`balanced`/`fast` (`model:"*"` + clientside auth) + `"*"` catch-all; the encrypted in-app registry resolves each tier → endpoint + key per call. Proven by a new litellm testcontainers test. Option B bootstrap (registry empty until HMI-configured). Cost moved to the registry (`build_registry_cost_catalog`). `.env.secret.example` ×2: `ANTHROPIC_API_KEY` → `AY_SECRET_MASTER_KEY`. CI 2245 passed (only an unrelated K8s-cert env error). Deploy pending a docker-desktop kubeconfig client-cert refresh.
- `.claude/sessions/2026-08-17-realtime-agentic-transparency.md` — **Real-time agentic transparency + per-request cost breakdown**. Claude-Code-like "watch the agent work" view in 3 verified/deployed increments over the append-only TraceEvent/InlineEvent stream: Inc 1 live SSE runs (`GET /orchestrator/runs/{id}/events` replay-then-tail → `liveTrace` on the pipeline page), Inc 2 extended-thinking + verbosity toggle (C8 `_apply_adaptive_thinking`, C3 `_extract_delta_reasoning` → `reasoning` inline events, C2 pref `reasoning_verbosity`, 🧠 toggle), Inc 3 sub-agent tree (`TraceEvent.sub_agent_id`/`parent_agent` → `buildSubAgentTree` + `<SubAgentTree>` + Timeline/Tree toggle). Part B: per-request cost by model, stored+viewable (`CallTags.turn_id`, C8 X-Run-Id/X-Turn-Id + sub-agent runtime run_id, `RequestCostBreakdown`, `GET /admin/v1/quota/requests/{correlation}/breakdown?by=run|turn`, `/operator/quotas` panel). 200-SPEC v4 / 800-SPEC v12 ; R-200-206/207/208 + R-800-146/147 = tested. Backend All-OK + UI 435/4-thresholds ; redeployed each block.
- `.claude/sessions/2026-08-04-operator-cost-storage-rights-dashboards.md` — **Operator resource dashboards (E-100-002 v7): cost + storage + rights**. Per-project/tenant/user LLM cost (day→year, c8 quota `consumption_by_project|user` + day-anchored reports) ; per-project/tenant disk storage (NEW `c8_llm/storage/` — MinIO metering by the `c4-artifacts/{tenant}/{project}/` layout + `storage_snapshots` series + `c8-storage-metering` CronJob every 6h + a "Snapshot now" trigger) ; reverse ACL view `GET /admin/users/{id}/projects`. Endpoints `GET /admin/v1/quota/consumption/{projects,users,tenants}` + `/admin/v1/storage/{projects,tenants}[/…/series]`, operator-scoped. UI cost/storage columns on Projects+Tenants+Users. Fixed 2 GitHub CI failures (testcontainers.community import + port-forward fd leak). settings v22 acceptEdits. R-800-145 + R-100-140 (traceability) ; 065=152. Backend All-OK + UI 4-thresholds ; redeployed.
- `.claude/sessions/2026-07-28-rbac-v7-content-blind-admin-and-catalogue-opt-out.md` — **RBAC E-100-002 v7 (content-blind tenant admin) + LLM catalogue opt-out (800 v10)**. Dropped `tenant_manager` ; `admin`(=`tenant_admin`) = content-blind tenant-scoped mirror of `platform_manager` (governance only ; C4-C7 strip admin before content gates ; admin gets operator Projects+Users console, backend-scoped). Closed a C2 `/auth/users` cross-tenant CRUD isolation hole (create forces caller tenant ; `{id}` routes 403 cross-tenant). LLM tenant catalogue → opt-out (lazy-materialised from the registry + `initialized` marker + `GET /catalog/available` re-add picker), fixing the empty-catalogue dead-end + unblocking `model_quality` resolution. 100-SPEC v20 (6→5 roles) / 800-SPEC v10 / 065=144. Backend All-OK 2141 + UI 424/4-thresholds ; K8s rebuilt+redeployed (+C2 re-seed).
- `.claude/sessions/2026-07-28-project-governance-and-platform-manager-rename.md` — **Project governance (E-100-002 v4) end-to-end + role rename `tenant_manager`→`platform_manager` (v5, BREAKING)**. Platform-operator cross-tenant project governance: inc1 C2 backend (`/admin/projects` list/status/ACL + `c2_audit`), inc2 UI `operator/projects` (status+ACL+consumption), inc3 URL-scoped 403 at `/verify` (method-aware inactive/archived), inc3b record-derived C3-conversation enforcement (read-only `c2_projects` status probe), consumption via `?project_id=`. THEN full role rename (74 files scripted, §5.2 exception + `git diff`; enum+value+gates+catalog+UI+env+specs+test-filename; generated regenerated). CI baseline restored first (tree-sitter 0.25 + fastapi 0.139 lazy `_IncludedRouter` → `tests/fixtures/routes.py`). E-100-002 **v4→v5**, 100-SPEC **v17→v18**. Tooling `run.sh --restart` + `k8s_reseed_c2.sh` (Job wipe). K8s rebuilt+redeployed+re-seeded (superroot=`platform_manager` verified live). Backend All-OK + UI 421/4-thresholds. NOT renamed: `.claude/sessions/*`, SESSION-STATE history, CLAUDE.md §13 (accurate records of the old name).
- `.claude/sessions/2026-06-08-llm-governance-lots-1-2-3.md` — **LLM-governance operator console: Lots 1·2·3 + self quota view**. L1 registry CRUD + api_base per-call injection (key-less) + sortable table + super-root home fix ; L2 tenant (de)activation + cross-tenant user oversight → E-100-002 v2→v3 (tenant_manager = platform operator) ; L3 global LLM quota policy (rolling windows, $ AND token limits, soft→hard `QuotaGuard` at the C8 gateway, usage from `llm_calls`) ; self view `GET /api/v1/quota/me` + navbar quota pill (everywhere). Operator UI under `/operator/*` (catch-all). 800-SPEC v5 / 100-SPEC v16-E-100-002 v3 / 065=128. Backend 1931 + UI 47/410 > thresholds.
- `.claude/sessions/2026-06-04-rag-followups-and-llm-governance-plan.md` — **RAG follow-ups (A1/B/C) + LLM-governance design & crypto brick**. A1 image captions folded into text before chunking ; B `run_stats.json` per-phase observability + UI ; B-ii authoritative $ cost via C8 (run_id/source_id headers → `llm_calls.tags` → C7 SUM → `enrichment_cost_usd` + UI) ; C document summary on standard. Designed the multi-tenant LLM-governance feature (registry + tenant catalog + `model_quality` + HMI-editable encrypted keys ; locked: app-level registry, no KMS/env master key, option-B per-call injection, code-first) ; shipped `crypto/SecretCipher` (AES-256-GCM, env keyring, AAD, rotation ; 13 tests). Backend All-OK (1748) + C13 551 + UI 373. Handoff brief written.
- `.claude/sessions/2026-06-03-rag-quality-and-cost-optimization.md` — **RAG quality + ingestion cost optimisation**. Contextual retrieval (`section_path` embedding + contextual BM25 `search_text` ; `global_summary` de-duped to source ; full chunk display ; c3 de-truncation) ; structure-aware chunking option B (real section extents + boundary cuts + LCP `section_path`) ; legacy audit (7 stale D-020 failures fixed + `DocumentPipeline`/`runner` dead code removed) ; **4-lever cost cut $4.53→~$1** (Haiku routing + vision trim + **map-reduce summariser redesign** + reusable prompt-cache primitive) + progress logs + `e2e_stack.sh restart-llm`. 548 C13 + backend All-OK + UI 373/gate.
- `.claude/sessions/2026-06-02-c13-enrichment-activation.md` — **C13 enrichment activated** : per-chunk orchestrator + image bytes/sha256-dedup/vision (independent model) + C7-owned `EnrichmentConfig` (UI→C7→C12→C13) + Claude routing (embeddings stay Ollama). catalog 111.
- **D-020 AyExtractor → C13** (2026-05-28 ×5 + 06-01/05-29) — adopted as dependency component (strip ~60% LOC, FastAPI HTTP wrapper, MinIO-as-contract, n8n `extract_and_ingest`) ; re-partitions D-013 ; **byte custody re-homed C12→C7** (R-100-081 v3, multipart `/sources/upload` + `/ingest-chunks`) ; anti-drift UI↔backend contract-test tier. See `sessions/2026-05-28-d020-*` + `2026-06-01-d020-*` + `2026-05-29-upload-405-*`.
- **V2 #1/#2/#3** (2026-05-22/23) — C8 LiteLLM proxy + per-agent Claude routing + cost tracking (`llm_calls`) ; OpenHands `generate` POC seam (gated) ; **Graphiti-style ArangoDB-native memory** (closed ontology + L1 extractor + hybrid BM25+dense RRF + contextualisation + bi-temporal D-019 ; eval recall@3=1.0 → D-010 closed, B unjustified). See `sessions/2026-05-22-*` + `2026-05-23-*`.
- **Earlier** (2026-04-22 → 2026-05-27) — K8s local bring-up (`2026-05-27-k8s-*`, 8 manifest bugs, `localhost:56000`) ; dev-stack/LiteLLM + runtime diagram ; DocGen per-response versioning + UI tranches ; UX chat finalisation + auth shell ; backbone C1-C9/C12 + CI/CD + observability + auth-matrix + Plan v1 A→F. Browse `.claude/sessions/`.

---

## 7. Maintenance rules

- This file SHALL remain ≤ 150 lines.
- Claude SHALL propose an update at end of any session introducing a decision, completing a stage, or changing §5.
- User validates before each write (no silent edits) except trivial deltas per `CLAUDE.md` §9.1.
