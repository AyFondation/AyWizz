# =============================================================================
# File: service.yaml
# Version: 2
# Path: infra/k8s/base/c7_memory/service.yaml
# Description: ClusterIP Service for C7 Memory.
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: c7-memory
  labels:
    app.kubernetes.io/name: c7-memory
    app.kubernetes.io/component: c7
    app.kubernetes.io/part-of: aywizz-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: c7-memory
    app.kubernetes.io/component: c7
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
