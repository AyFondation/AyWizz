# =============================================================================
# File: deployment.yaml
# Version: 4
# Path: infra/k8s/base/c4_orchestrator/deployment.yaml
# Description: C4 Orchestrator Deployment. Shared `aywizz-api` image,
#              runtime selected via `COMPONENT_MODULE=c4_orchestrator`.
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: c4-orchestrator
  labels:
    app.kubernetes.io/name: c4-orchestrator
    app.kubernetes.io/component: c4
    app.kubernetes.io/part-of: aywizz-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: c4-orchestrator
      app.kubernetes.io/component: c4
  template:
    metadata:
      labels:
        app.kubernetes.io/name: c4-orchestrator
        app.kubernetes.io/component: c4
        app.kubernetes.io/part-of: aywizz-platform
    spec:
      # The identity the K8sDispatcher acts as. It carries NO rights on its
      # own; the opt-in `c4_workers` layer binds it to a namespaced Role
      # granting Pod create/get/list/watch/delete where sandboxes run
      # (`c4_workers/rbac.yaml`). Declared here, not there, so the SA always
      # exists alongside the Deployment that references it — including with
      # the default in-process dispatcher, where it holds nothing.
      serviceAccountName: c4-orchestrator
      containers:
        - name: api
          image: ghcr.io/ayfondation/aywizz-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: COMPONENT_MODULE
              value: c4_orchestrator
            - name: PYTHONUNBUFFERED
              value: "1"
          envFrom:
            - configMapRef:
                name: aywizz-config
            - secretRef:
                name: aywizz-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 20
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            limits:
              cpu: "400m"
              memory: "512Mi"
            requests:
              cpu: "25m"
              memory: "128Mi"
